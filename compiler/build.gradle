apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    compileOnly project(':messagebox-annotation')
    implementation 'com.squareup:javapoet:1.10.0'
}

sourceCompatibility = "8"
targetCompatibility = "8"
